pipeline {

    agent any

    stages {

        stage('test') {
            echo 'test'
        }

        stage('build') {
            echo "build"
        }

        stage "deploy" {
            echo 'deploy'
        }

    }

}